import classNames from 'classnames/bind';

import styles from './Register.module.scss';

const cx = classNames.bind(styles);

const Register = () => {
    return <div className={cx('register')}>Register Page</div>;
};

export default Register;
